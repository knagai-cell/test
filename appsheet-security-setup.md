# AppSheet 日報アプリ — セキュリティ & 自動化 設定ガイド

## 対象スプレッドシート

Google Sheets URL: `https://docs.google.com/spreadsheets/d/1FS39RlXQbVcnl8kCu_h-FnuiBRW5uJ3cUj3unm92kYk/`

## テーブル構成（日報テーブル）

| # | カラム名 | 型 | 入力者 | 備考 |
|---|---------|-----|--------|------|
| 1 | 日付 | Date | 営業マン | |
| 2 | 氏名 | Text | 営業マン | |
| 3 | 当日接触数 | Number | 営業マン | |
| 4 | アポ数 | Number | 営業マン | |
| 5 | 商談数 | Number | 営業マン | |
| 6 | 申込数 | Number | 営業マン | |
| 7 | 成約数 | Number | 営業マン | |
| 8 | アポ率(%) | Percent | 自動計算 | アポ数 / 当日接触数 |
| 9 | 成約率(%) | Percent | 自動計算 | 成約数 / 商談数 |
| 10 | 今日の成功 | LongText | 営業マン | |
| 11 | 今日の失敗 | LongText | 営業マン | |
| 12 | 明日の改善 | LongText | 営業マン | |
| 13 | 明日の行動予定 | LongText | 営業マン | |
| 14 | ボトルネック(月次改善目標) | LongText | 営業マン | |
| 15 | 理由 | LongText | 営業マン | |
| 16 | 上長コメント | LongText | **上長のみ** | Editable制限あり |
| 17 | メールアドレス | Email | 営業マン | Security Filterで使用 |

## 全体構成

```
ログイン(Security Filter) → 権限制御(Column制御) → 自動通知(Bot) → 管理画面(View) → 運用開始
```

| ロール | できること |
|--------|-----------|
| **Admin（上長）** | 全員の日報を閲覧・上長コメント編集・管理画面アクセス |
| **User（営業マン）** | 自分の日報のみ閲覧・上長コメント編集不可 |

## カラム別 権限マトリックス

| カラム | User（営業マン） | Admin（上長） |
|--------|-----------------|--------------|
| 日付〜理由（15カラム） | 閲覧・編集可 | 閲覧・編集可 |
| 上長コメント | 閲覧のみ（空なら非表示） | 閲覧・編集可 |
| メールアドレス | 閲覧・編集可 | 閲覧・編集可 |

---

## ステップ1：Security Filter（ログイン機能）

**目的：** ログインしたメールアドレスに一致する行だけを表示させる

### 設定手順

1. 左メニューの **[Security]**（鍵アイコン）> **[Security Filters]** を開く
2. **「日報」テーブル**を選択
3. **Filter condition** に以下の数式を入力：

```
OR(USERROLE() = "Admin", [メールアドレス] = USEREMAIL())
```

### 数式の解説

| 部分 | 意味 |
|------|------|
| `USERROLE() = "Admin"` | 管理者（上長）なら全行表示 |
| `[メールアドレス] = USEREMAIL()` | 一般ユーザーなら自分のメアド行のみ表示 |
| `OR(...)` | どちらかが TRUE なら表示 |

### 管理者ロールの設定

1. 左メニュー **[Users]** を開く
2. 上長のメールアドレスの横にある **Role** を **「Admin」** に変更
3. 保存

### 確認方法

- エディタ上部の **「Preview as」** に営業マンのメアドを入力
- その人の視点でアプリを確認し、他人のデータが見えないことを検証

---

## ステップ2：上長コメントの制限

**目的：** 営業マンが「上長コメント」を編集・閲覧できないようにする

### 2-1. 編集制限（Editable?）

1. **[Data]** > **[Columns]** で「上長コメント」の **鉛筆アイコン** をクリック
2. **[Update Behavior]** 欄の **Editable?** のフラスコアイコンに以下を入力：

```
USERROLE() = "Admin"
```

> これにより、Admin以外はタップしても入力不可になります。

### 2-2. 表示制限（Show?）— オプション

コメントが空の時は営業マンに見せたくない場合、**Show?** 欄に以下を入力：

```
OR(USERROLE() = "Admin", ISNOTBLANK([上長コメント]))
```

| 状態 | Admin | User |
|------|-------|------|
| コメントあり | 表示・編集可 | 表示のみ（編集不可） |
| コメント空 | 表示・編集可 | 非表示 |

---

## ステップ3：自動通知Bot（コメント通知）

**目的：** 上長がコメントを保存した瞬間に、該当する営業マンへ通知を飛ばす

### 3-1. Bot作成 & Trigger設定

1. 左メニュー **[Automation]** > **[Create a new Bot]**
2. **Event Type：** `UPDATES_ONLY`（更新のみ）
3. **Condition** に以下の数式を入力：

```
AND(
  ISNOTBLANK([上長コメント]),
  [_THISROW_BEFORE].[上長コメント] <> [_THISROW_AFTER].[上長コメント]
)
```

### Trigger数式の解説

| 部分 | 意味 |
|------|------|
| `ISNOTBLANK([上長コメント])` | コメント欄が空でないこと |
| `[_THISROW_BEFORE].[上長コメント]` | 更新前の値 |
| `[_THISROW_AFTER].[上長コメント]` | 更新後の値 |
| `<>` | 値が変わった（書き換えられた）時のみ |

### 3-2. 通知Step設定

1. **Run a Task** > **Send a notification**（スマホ通知）または **Send an email**
2. 各項目を設定：

| 項目 | 設定値 |
|------|--------|
| **To** | `[メールアドレス]` ← その行の営業マンのアドレス |
| **Title** | `上長からコメントが届きました` |
| **Body** | `[上長コメント]` |

---

## ステップ4：上長専用の管理画面

**目的：** 10名分を一覧できる画面を上長だけが見えるようにする

### 設定手順

1. **[App]** > **[Views]** で新しいViewを作成（例：`管理用一覧`）
2. **Display** 欄の **Show if** に以下を入力：

```
USERROLE() = "Admin"
```

3. View Type を **Table** または **Deck** に設定（一覧表示向き）

> これにより、営業マンのスマホにはこのメニュー自体が表示されなくなります。

---

## 運用：ユーザー追加 & テスト

### ユーザー追加

1. 左メニュー **[Users]** を開く
2. 10名のメールアドレスを追加
3. 各ユーザーの **Role** を **「User」** に設定
4. 招待を送信

### テスト手順チェックリスト

| # | テスト項目 | 確認方法 |
|---|-----------|---------|
| 1 | 営業マンは自分の日報のみ見えるか | Preview asで営業マンのメアドを入力 |
| 2 | 営業マンは他人の日報が見えないか | 同上で他人のデータが消えていることを確認 |
| 3 | 営業マンは上長コメントを編集できないか | Preview asで上長コメント欄をタップ |
| 4 | 上長コメントが空の時、営業マンに非表示か | (Show?設定時のみ) |
| 5 | 上長がコメント保存で通知が届くか | 実際にコメントを入力して保存 |
| 6 | 管理画面が営業マンに見えないか | Preview asで確認 |
| 7 | 管理画面がAdminに見えるか | Preview asでAdminメアドを入力 |

---

## 使用する主なAppSheet関数リファレンス

| 関数 | 説明 |
|------|------|
| `USEREMAIL()` | ログイン中のユーザーのメールアドレスを返す |
| `USERROLE()` | ログイン中のユーザーのロール（Admin/User等）を返す |
| `ISNOTBLANK(value)` | 値が空でなければ TRUE を返す |
| `OR(条件1, 条件2)` | いずれかが TRUE なら TRUE |
| `AND(条件1, 条件2)` | すべてが TRUE なら TRUE |
| `[_THISROW_BEFORE]` | 更新前の行データを参照（Automation内で使用） |
| `[_THISROW_AFTER]` | 更新後の行データを参照（Automation内で使用） |
